# Simple Chatbot

## Overview

This project is a simple rule-based chatbot built using Python and Streamlit. It can answer basic conversational questions and demonstrates the fundamentals of chatbot development.

## Features

* Friendly conversation
* Rule-based responses
* Simple web interface
* Lightweight and beginner-friendly

## Technologies Used

* Python
* Streamlit

## Installation

Clone the repository:

```bash
git clone https://github.com/yourusername/simple-chatbot.git
cd simple-chatbot
```

Install dependencies:

```bash
pip install -r requirements.txt
```

Run the application:

```bash
streamlit run app.py
```

## Approach

This chatbot uses a rule-based approach. User inputs are matched against predefined keywords and responses. This method is simple, fast, and does not require external APIs or machine learning models.

## Challenges Faced

* Designing meaningful responses for different user inputs.
* Handling unknown questions gracefully.

## Future Improvements

* Add AI-powered responses using Gemini or OpenAI APIs.
* Implement chat history.
* Add voice input and output.

## Demo

Record a short screen capture showing:

1. Opening the chatbot.
2. Sending messages.
3. Receiving responses.
4. Closing the conversation.
